<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quiosco AW</title>

    <!-- BOOSTRAP -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">

    <!-- CSS -->
    <link rel="stylesheet" href="../../css/main.css">
</head>
<body>
    <%- include ("partials/navegacion") %>

    <div class="container p-5">
        <div class="row">
          <div class="col-sm-8 offset-md-5 mx-auto">
            <div class="card">

                <div class="card-header bg-dark text-white text-center">

                    <% if(numeroCromos == 0) { %>
                        <h3>Colección: <%= nombreColeccion %> - vacio</h3>

                    <% } else { %>
                        <h3><%= nombreColeccion %> - <%= cromosComprados %> de <%= cromosTotal %></h3>
                    <% } %>

                </div>

                <div class="card-body text-center">  

                    <% if(numeroCromos == 0) { %>
                        <h1>No hay ningún cromo comprado</h1>

                    <% } else { %>

                        <table class="table table-dark">

                        <tbody class="text-center">
                            <tr>
                                <% for(var i=0; i < numeroCromos; i++) {%>
                                    <td>
                                        <!--<img src="<%= cromos[i].cromoImagen %>" class="img-fluid">-->
                                        <h3><%= cromos[i].cromoImagen %></h3>
                                        <br>
                                        <%= cromos[i].cromoNombre %>
                                    </td>
        
                                    <% if(i != 0 && (i+1)%4 == 0) {%>
                                        </tr>
                                        <tr>
                                    <% }%>
                                <% } %>
                            </tr>
                        </tbody>
                        </table>
                    <% } %>
                </div>

                <div class="card-body">
                    <button type="button" class="btn btn-success btn-block" onclick="location.href='/tienda'">Ir a la tienda</button>
                </div>
            </div>
          </div>
        </div>
    </div>     
</body>
</html>